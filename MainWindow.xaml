<Window x:Class="DailyToDo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DailyToDo"
        xmlns:vm="clr-namespace:DailyToDo.ViewModels"
        mc:Ignorable="d"
        Title="Daily ToDo" Height="600" Width="350"
        WindowStartupLocation="CenterScreen">
    
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CornerRadius="0" CaptionHeight="32"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Colors -->
        <Color x:Key="BrandColor">#2B88D8</Color>
        <SolidColorBrush x:Key="TextPrimary" Color="#333333"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#767676"/>
        
        <!-- Style for the Circular Checkbox -->
        <Style x:Key="RoundCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Ellipse x:Name="OuterCircle" Stroke="#737373" StrokeThickness="1.5" Width="22" Height="22" Fill="Transparent"/>
                            <Path x:Name="CheckMark" Data="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" 
                                  Fill="Transparent" Width="14" Height="14" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="OuterCircle" Property="Fill" Value="#737373"/>
                                <Setter TargetName="OuterCircle" Property="Stroke" Value="#737373"/>
                                <Setter TargetName="CheckMark" Property="Fill" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterCircle" Property="Stroke" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>



        <!-- Window Control Button Styles -->
        <Geometry x:Key="IconPin">M16,12V4H17V2H7V4H8V12L6,14V16H11V22H13V16H18V14L16,12Z</Geometry>
        <Geometry x:Key="IconMinimize">M19,13H5V11H19V13Z</Geometry>
        <Geometry x:Key="IconClose">M19,6.41L17.59,5 12,10.59 6.41,5 5,6.41 10.59,12 5,17.59 6.41,19 12,13.41 17.59,19 19,17.59 13.41,12Z</Geometry>

        <Style x:Key="WindowButtonStyle" TargetType="ButtonBase">
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ButtonBase">
                        <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#33FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WindowCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WindowPinToggleStyle" TargetType="ToggleButton" BasedOn="{StaticResource WindowButtonStyle}">
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Foreground" Value="Orange"/>
                </Trigger>
                 <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4CFFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <!-- Background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#006990" Offset="0.0"/>
                <GradientStop Color="#004660" Offset="0.5"/>
                <GradientStop Color="#002030" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Tasks -->
            <RowDefinition Height="Auto"/> <!-- Input -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="20,40,20,10">

            <TextBlock Text="我的一天" FontSize="30" FontWeight="Bold" Foreground="White">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="1" Opacity="0.3"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="{Binding CurrentDate}" FontSize="13" Foreground="White" Opacity="0.8" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Task List -->
        <ScrollViewer Grid.Row="1" Margin="10,10,10,0" VerticalScrollBarVisibility="Hidden">
            <ItemsControl ItemsSource="{Binding Tasks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="5" Margin="0,0,0,8" Height="58">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="5" ShadowDepth="1" Direction="270"/>
                            </Border.Effect>
                            <Grid Margin="12,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>

                                </Grid.ColumnDefinitions>

                                <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted}" Style="{StaticResource RoundCheckBoxStyle}" VerticalAlignment="Center"/>
                                
                                <TextBlock Grid.Column="1" Text="{Binding Title}" VerticalAlignment="Center" Margin="15,0,10,0" 
                                           FontSize="15" TextTrimming="CharacterEllipsis">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                                    <Setter Property="Opacity" Value="0.6"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Bottom Add Bar -->
        <Border Grid.Row="2" Margin="15,10,15,20" Height="50" Background="#33000000" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Add Button -->
                <Button Grid.Column="0" Command="{Binding AddTaskCommand}" Background="Transparent" BorderThickness="0" Margin="10,0,0,0" VerticalAlignment="Center">
                    <Path Data="M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z" Fill="White" Width="20" Height="20" Stretch="Uniform"/>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- Input Area with Placeholder -->
                <Grid Grid.Column="1" Margin="10,0,10,0" VerticalAlignment="Center">
                    <TextBlock Text="添加任务" Foreground="#CCFFFFFF" FontSize="15" IsHitTestVisible="False" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding NewTaskTitle}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding NewTaskTitle}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBox Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="15" Background="Transparent" BorderThickness="0" Foreground="White" CaretBrush="White">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding AddTaskCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                </Grid>
            </Grid>
        </Border>

        <!-- Window Control Buttons -->
        <StackPanel Grid.Row="0" Grid.RowSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
            <ToggleButton Click="PinBtn_Click" Style="{StaticResource WindowPinToggleStyle}" ToolTip="置顶">
                <Path Data="{StaticResource IconPin}" Fill="{Binding RelativeSource={RelativeSource AncestorType=ToggleButton}, Path=Foreground}" Stretch="Uniform" Width="10" Height="10"/>
            </ToggleButton>
            <Button Click="MinimizeBtn_Click" Style="{StaticResource WindowButtonStyle}" ToolTip="最小化">
                <Path Data="{StaticResource IconMinimize}" Fill="White" Stretch="Uniform" Width="10" Height="10"/>
            </Button>
            <Button Click="CloseBtn_Click" Style="{StaticResource WindowCloseButtonStyle}" ToolTip="关闭">
                <Path Data="{StaticResource IconClose}" Fill="White" Stretch="Uniform" Width="10" Height="10"/>
            </Button>
        </StackPanel>

    </Grid>
</Window>
